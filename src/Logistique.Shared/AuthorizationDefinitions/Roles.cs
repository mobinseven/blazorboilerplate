using System;
using System.Collections.Generic;
using System.Text;

namespace Logistique.Shared.AuthorizationDefinitions
{
    public static class Roles
    {
        public const string Manager = nameof(Manager);
        public const string Driver = nameof(Driver);
    }
}